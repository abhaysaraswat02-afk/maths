rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // All access is server-side only via Admin SDK
      allow read, write: if false;
    }
  }
}
